# Theme Gallery

Theme contribution sample inspired by popular Obsidian themes. It contributes ten unique LightPaper theme IDs plus `themes.css`, a scoped stylesheet that changes app tokens, editor spacing, preview typography, code blocks, callouts, tables, and active-line treatment after the plugin is installed and enabled.
