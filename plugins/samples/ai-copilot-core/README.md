# AI Copilot Core

First-party AI writing workflows for summaries, tags, rewrites, outlines, critique, and continuation.

The sample handlers are deterministic and offline for tests. When a model provider is selected, the active provider/model config is passed into each preset input.
