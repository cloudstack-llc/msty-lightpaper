# Markdown Power Pack

Curated Markdown power tools: wiki-link preview support, callout insertion, lint reports, frontmatter-safe normalization, and table formatting.

This sample intentionally reuses smaller sample plugin helpers so the bundle behavior and the individual capabilities remain separately testable.
