# Model Provider Catalog

Registers provider and model configuration records for AI workflows.

This sample intentionally stores API key references such as `secret://providers/openai/api-key`, not raw API keys. A production secret store can resolve those references later through Electron or the OS keychain.
